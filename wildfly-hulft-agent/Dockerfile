FROM jboss/wildfly:18.0.0.Final

# Java 8 is already present in the base image

# Placeholder: Install Hulft 8 (manual or script installation required)
# COPY hulft8-installer.sh /tmp/
# RUN bash /tmp/hulft8-installer.sh

# Placeholder: Install Hinemos Agent (manual or script installation required)
# COPY hinemos-agent-installer.sh /tmp/
# RUN bash /tmp/hinemos-agent-installer.sh

CMD ["/opt/jboss/wildfly/bin/standalone.sh", "-b", "0.0.0.0"]
